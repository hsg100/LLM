// Test stub: the real `server-only` package throws outside a React Server
// environment. Vitest renders server components as plain functions, so the
// marker is neutralised here (the production build still enforces it).
export {};
